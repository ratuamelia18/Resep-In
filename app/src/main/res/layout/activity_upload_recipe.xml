<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5E6DA"
    tools:context=".ui.theme.UploadRecipeActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <!-- Judul -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Upload Resep"
            android:textSize="28sp"
            android:textStyle="bold"
            android:textColor="#5D4037"
            android:layout_gravity="center"
            android:layout_marginBottom="24dp" />

        <!-- Nama Resep -->
        <EditText
            android:id="@+id/etRecipeName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Nama Resep"
            android:padding="14dp"
            android:background="@android:drawable/edit_text"
            android:layout_marginBottom="14dp" />

        <!-- Deskripsi -->
        <EditText
            android:id="@+id/etDescription"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:hint="Deskripsi Resep"
            android:gravity="top"
            android:padding="14dp"
            android:background="@android:drawable/edit_text"
            android:layout_marginBottom="14dp" />

        <!-- Bahan -->
        <EditText
            android:id="@+id/etIngredients"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:hint="Bahan-bahan"
            android:gravity="top"
            android:padding="14dp"
            android:background="@android:drawable/edit_text"
            android:layout_marginBottom="14dp" />

        <!-- Langkah -->
        <EditText
            android:id="@+id/etSteps"
            android:layout_width="match_parent"
            android:layout_height="150dp"
            android:hint="Langkah-langkah Memasak"
            android:gravity="top"
            android:padding="14dp"
            android:background="@android:drawable/edit_text"
            android:layout_marginBottom="14dp" />

        <!-- Upload Foto -->
        <Button
            android:id="@+id/btnUploadImage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Upload Foto"
            android:textAllCaps="false"
            android:backgroundTint="#D7B49E"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="10dp" />

        <!-- Preview Foto -->
        <ImageView
            android:id="@+id/imgPreview"
            android:layout_width="match_parent"
            android:layout_height="180dp"
            android:scaleType="centerCrop"
            android:src="@android:drawable/ic_menu_gallery"
            android:background="#EAD7C7"
            android:layout_marginBottom="16dp" />

        <!-- Upload Video -->
        <Button
            android:id="@+id/btnUploadVideo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Upload Video"
            android:textAllCaps="false"
            android:backgroundTint="#D7B49E"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="10dp" />

        <!-- Nama File Video -->
        <TextView
            android:id="@+id/tvVideoName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Belum ada video dipilih"
            android:textColor="#5D4037"
            android:textSize="14sp"
            android:layout_marginBottom="20dp" />

        <!-- Tombol Simpan -->
        <Button
            android:id="@+id/btnSaveRecipe"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Simpan Resep"
            android:textStyle="bold"
            android:textAllCaps="false"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:padding="14dp"
            android:backgroundTint="#A47551" />

    </LinearLayout>

</ScrollView>